<header id="main-nav">
  <div class="nav-top">
    <span class="sitename"><a href="/">CSE Motors</a></span>
    <div id="tools">
      <% if (locals.user) { %>
        <a href="/account/logout">Logout</a>
      <% } else { %>
        <a href="/account/login">My Account</a>
      <% } %>
    </div>
    <button id="nav-toggle" aria-label="Toggle Navigation">&#9776;</button>
  </div>
  <%- include('navigation') %>

  <!-- Keep the forms.css link included -->
  <link rel="stylesheet" href="/css/forms.css" />
</header>
